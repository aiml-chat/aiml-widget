{
  "name": "aiml-widget",
  "version": "1.2.0",
  "description": "Embeddable AI chat widget for any website",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.cjs",
    "build:watch": "node build.cjs --watch",
    "test": "node --import ./tests/register-css-stub.mjs --test tests/**/*.test.mjs",
    "size": "node size.cjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "jsdom": "^29.1.1"
  },
  "files": [
    "dist/"
  ]
}
